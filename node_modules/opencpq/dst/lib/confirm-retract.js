"use strict";

var React = require("react");

var _require = require("react-bootstrap");

var Button = _require.Button;
var Glyphicon = _require.Glyphicon;

function confirmOrRetractButton(userSelected, confirm, retract) {
	var size = arguments.length <= 3 || arguments[3] === undefined ? "medium" : arguments[3];

	return userSelected ? React.createElement(
		Button,
		{ className: "dim", bsSize: size, onClick: retract },
		React.createElement(Glyphicon, { glyph: "remove" })
	) : React.createElement(
		Button,
		{ className: "dim", bsSize: size, onClick: confirm },
		React.createElement(Glyphicon, { glyph: "ok" })
	);
}

module.exports = { confirmOrRetractButton: confirmOrRetractButton };
//# sourceMappingURL=../maps/lib/confirm-retract.js.map